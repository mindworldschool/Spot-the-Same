# Find it First - Embed Code

## Правильный код для встраивания игры на ваш сайт

### Проблема

Старый код iframe определял язык только из атрибута `document.documentElement.lang`, что не работало когда пользователи заходили по URL типа `/en/finditfirst`.

### Решение

Новый код (в файле `EMBED.html`) определяет язык в следующем порядке приоритета:

1. **URL путь родительской страницы** (приоритет 1)
   - `/en/finditfirst` → английский
   - `/ru/finditfirst` → русский
   - `/es/finditfirst` → испанский
   - `/finditfirst` (без префикса) → проверяет дальше

2. **Атрибут `lang` в HTML элементе** (приоритет 2)
   - `<html lang="en">` → английский
   - `<html lang="ru">` → русский
   - `<html lang="ua">` → украинский
   - `<html lang="es">` → испанский

3. **Язык браузера** (приоритет 3)
   - `navigator.language = 'en-US'` → английский
   - `navigator.language = 'ru-RU'` → русский
   - `navigator.language = 'uk-UA'` → украинский
   - `navigator.language = 'es-ES'` → испанский

4. **По умолчанию украинский** (fallback)

## Как использовать

Скопируйте весь код из файла `EMBED.html` и вставьте на вашу страницу где хотите показать игру.

## Что изменилось

### Старый код (неправильный):
```javascript
var lang = document.documentElement.lang || 'ua';
lang = lang.toLowerCase();

if (lang.includes('uk') || lang.includes('ua')) lang = 'ua';
else if (lang.includes('en')) lang = 'en';
// ... и т.д.
```

**Проблема**: не проверяет URL путь

### Новый код (правильный):
```javascript
// 1. Сначала проверяем URL путь
var currentPath = window.location.pathname.toLowerCase();
if (currentPath.includes('/en/')) lang = 'en';
// ... и т.д.

// 2. Потом проверяем атрибут lang
if (!lang) {
  var htmlLang = document.documentElement.lang || '';
  // ... проверка атрибута
}

// 3. Потом проверяем язык браузера
if (!lang) {
  var browserLang = navigator.language;
  // ... проверка языка браузера
}

// 4. По умолчанию украинский
if (!lang) lang = 'ua';
```

## Логирование

Новый код выводит подробные логи в консоль браузера для отладки:

```
🌍 [Find it First Embed] Checking current path: /en/finditfirst
🌍 [Find it First Embed] Language detected from URL path: en
🌍 [Find it First Embed] Final game URL: https://mindworldschool.github.io/Find-it-First/?lang=en
```

## Структура URL игры

Игра поддерживает языки через параметр `?lang=`:
- `?lang=ua` - украинский (по умолчанию)
- `?lang=en` - английский
- `?lang=ru` - русский
- `?lang=es` - испанский

## Примеры использования

### Страница с языком в URL
```
Родительская страница: https://example.com/en/finditfirst
Iframe загрузит: https://mindworldschool.github.io/Find-it-First/?lang=en
```

### Страница с атрибутом lang
```html
<html lang="en">
<!-- Iframe загрузит игру на английском -->
```

### Страница без языковых меток
```
Браузер установлен на английский → игра на английском
Браузер установлен на украинский → игра на украинском
Неизвестный язык → игра на украинском (по умолчанию)
```

## Замечания

- Украинский язык НЕ использует префикс `/ua/` в URL - это default язык без префикса
- Только другие языки (`/en/`, `/ru/`, `/es/`) используют префиксы в URL
- Код полностью автономный и не требует дополнительных зависимостей
- Console логи помогают отладить определение языка
